import NavBar from "./NavBar";

const Header = (props) => {
  console.log(props, 'divesh pro');
  
    return (
      <header>
        <NavBar />
      </header>
    );
  };
  
  export default Header;